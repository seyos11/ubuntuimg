apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "linux.fullname" . }}-test-connection"
  labels:
    {{- include "linux.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "linux.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "linux.fullname" . }}
  labels:
    {{- include "linux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "linux.selectorLabels" . | nindent 4 }}

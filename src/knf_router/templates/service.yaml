apiVersion: v1
kind: Service
metadata:
  name: {{ include "knf_router.fullname" . }}
  labels:
    {{- include "knf_router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knf_router.selectorLabels" . | nindent 4 }}

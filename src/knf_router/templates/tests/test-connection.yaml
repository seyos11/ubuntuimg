apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "knf_router.fullname" . }}-test-connection"
  labels:
    {{- include "knf_router.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "knf_router.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
